A simple bash script that updates Cloudflare DNS records with just Zone IDs and an API token.

- Add your Zone ID
- Add your API token
- Add the hostnames you want to update

Set it as a cronjob and call it a day.
